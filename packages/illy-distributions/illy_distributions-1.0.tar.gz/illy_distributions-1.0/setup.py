from setuptools import setup

setup(name='illy_distributions',
      version="1.0",
      description='Gaussian and Binomial distributions',
      packages=['illy_distributions'],
      author = 'illyShelly',
      zip_safe=False)
