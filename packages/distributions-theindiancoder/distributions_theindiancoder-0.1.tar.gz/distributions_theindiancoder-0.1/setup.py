from setuptools import setup

setup(name='distributions_theindiancoder',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_theindiancoder'],
      author='theindiancoder',
      zip_safe=True)
