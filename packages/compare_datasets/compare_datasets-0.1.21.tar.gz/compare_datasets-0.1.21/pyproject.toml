[tool.poetry]
name = "compare_datasets"
version = "0.1.21"
description = "The library that compares two dataframes"
authors = ["Kumar Shantanu"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8, <=3.12"
polars = ">=0.19.5"
tabulate = "^0.8.9"
jinja2 = "^3.1.2"
pandas = ">1.0.0"
tqdm = "^4.66.1"
Levenshtein = "^0.23.0"
scipy = "^1.10.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

# [tool.poetry.package]
# include = ["compare_datasets/resources/*"]