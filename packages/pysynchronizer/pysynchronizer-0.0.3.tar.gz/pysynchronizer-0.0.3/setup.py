from setuptools import setup, find_packages

setup(
    name="pyscynchronize",
    version="0.0.3",
    packages=find_packages(),
    python_requires=">=3.7",
    install_requires=[

    ],
    extras_require={

    }
)