[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "gguf-selector"
authors = [{name = "calcuis", email = "calculatics@gmail.com"}]
description = "GGUF selector"
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version"]
dependencies = [
    "llama-core"
]

[project.urls]
Homepage = "https://github.com/calcuis/gguf-selector"
Issues = "https://github.com/calcuis/gguf-selector/issues"