from setuptools import setup, find_packages

setup(
    name='Njalla',
    version='0.3',
    packages=find_packages(),
    install_requires=[
        'requests'
    ],
    entry_points={
        'console_scripts': [

        ],
    },
)
