from setuptools import setup

setup(name='liza_distributions_moscow',
      version='1.0',
      description='Gaussian distributions',
      packages=['liza_distributions_moscow'],
      zip_safe=False)
