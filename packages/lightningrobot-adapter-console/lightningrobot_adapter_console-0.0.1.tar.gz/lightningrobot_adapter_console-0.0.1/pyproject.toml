[tool.poetry]
name = "lightningrobot-adapter-console"
version = "0.0.1"
description = "Interact with your bot in the console."
authors = ["MeowSauce <meowsauce@hotmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
lightningrobot = "^0.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
