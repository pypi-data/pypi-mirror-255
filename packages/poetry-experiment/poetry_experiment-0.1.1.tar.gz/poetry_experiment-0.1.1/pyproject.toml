[tool.poetry]
name = "poetry-experiment"
version = "0.1.1"
description = "Testing poetry, Correct Python Packaging, installing and running as cli"
authors = ["Tami Bar <fire17@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
poetryexp = "poetry_experiment.program:main"
