from setuptools import setup, find_packages

setup(
    name='tutorialtestpackage3',  # Replace with your actual package name
    url = 'https://gitlab.com/AnoopANair/pypitest.git', # Replace with your github project link
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Your project dependencies
    ],
)
