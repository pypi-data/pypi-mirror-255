[tool.poetry]
name = "notifical"
version = "0.2.0"
description = "API to fire python code based off ical feeds"
authors = ["Ryan Doyle <ryan@rdoyle.net>"]
license = "BSD"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.31.0"
icalendar = "^5.0.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
