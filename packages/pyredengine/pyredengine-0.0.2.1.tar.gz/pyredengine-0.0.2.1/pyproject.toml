[project]
name = "pyredengine"
dynamic = ["0.0.3"]
dependencies = ['pygame-ce', 'pytweening']
requires-python = ">= 3.8, < 3.11"

[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"