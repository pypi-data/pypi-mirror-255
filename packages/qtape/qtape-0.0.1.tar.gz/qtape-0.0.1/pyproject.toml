[build-system]
requires = ["setuptools", "Cython", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "qtape"
version = "0.0.1"
authors = [
{name = "Brennan de Neeve", email = "brennan.mn@proton.me"},
]
description = "A python package for (quantum) time adaptive phase estimation"
readme = "README.rst"
dependencies = ["numpy"]
