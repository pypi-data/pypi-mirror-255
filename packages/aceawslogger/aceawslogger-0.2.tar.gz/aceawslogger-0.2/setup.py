from setuptools import setup, find_packages

setup(name='aceawslogger', 
      version='0.2', 
      author='ace',
      author_email='kardaras.george@clublabs.com',
      description='AWS Logger',
      packages=find_packages(),
      install_requires=[],
      )