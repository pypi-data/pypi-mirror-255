[project]
name = "npc-cleanup"
version = "0.1.0"
description = ""
authors = [
    { name = "Christopher", email = "chrism@alleninstitute.org" },
]
dependencies = [
    "npc-lims>=0.1.110",
    "npc-sessions>=0.0.179",
    "pydantic>=2.6.0",
    "pymongo>=4.6.1",
    "click>=8.1.7",
]
requires-python = ">=3.9"
readme = "README.md"

[project.license]
text = "MIT"

[project.optional-dependencies]
npc = []

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm.dev-dependencies]
dev = [
    "pytest>=8.0.0",
]
