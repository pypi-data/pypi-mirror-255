# setup.py
from setuptools import setup, find_packages

setup(
    name='smaran_alu',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here
    ],
)
