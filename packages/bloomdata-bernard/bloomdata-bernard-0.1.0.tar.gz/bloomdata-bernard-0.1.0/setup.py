from setuptools import setup, find_packages

setup(
    name='bloomdata-bernard',
    version='0.1.0',
    description='Your package description',
    author='Your Name',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'pandas',
    ],
)