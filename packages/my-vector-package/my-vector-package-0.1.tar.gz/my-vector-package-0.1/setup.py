from setuptools import setup, find_packages

setup(
    name='my-vector-package',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add any dependencies your package may have
    ],
)
