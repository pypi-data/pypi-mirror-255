from setuptools import setup, find_packages

setup(
    name='JscorpVite',
    version='1.0.3',
    packages=find_packages(),
    install_requires=[
        'Django',
        # Add other dependencies here
    ],
)
