[tool.poetry]
name = "copyfilter"
version = "0.1.0"
description = ""
authors = ["Ben Skubi <bskubi@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
click = "^8.1.7"
pytest = "^8.0.0"

[tool.poetry.scripts]
copyfilter = "copyfilter.__main__:copy_files"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
